# Variáveis de orquestração do docker-compose (raiz).
# Os segredos do backend (Telegram, OpenRouter, etc.) ficam em server/.env.

# URL pública do backend que o frontend embute no build (Vite)
VITE_API_URL=http://localhost:3333

# Portas publicadas no host
API_PORT=3333
WEB_PORT=8080
