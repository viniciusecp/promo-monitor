# Só lido pelo docker-compose. Rodando nativo, vale server/.env e web/.env.

# Porta publicada no host — o único ponto de acesso.
# Acesse em http://SEU_IP:${WEB_PORT}
WEB_PORT=5000

# O frontend chama /api na própria origem; não existe VITE_API_URL.
