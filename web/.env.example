# Normalmente desnecessário — em prod o nginx proxia, em dev o vite.config.ts.
# Defina só para apontar o painel a um backend em outro endereço.
# Nesse caso, adicione a origem do painel em CORS_ORIGINS (server/.env).
# VITE_API_URL=http://localhost:4999
