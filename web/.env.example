# Config do frontend para o modo dev (pnpm dev).
# No build via Docker, este arquivo é ignorado — lá o VITE_API_URL vem
# do build arg definido no .env da raiz.

# URL do backend que o frontend consome
VITE_API_URL=http://localhost:3333
